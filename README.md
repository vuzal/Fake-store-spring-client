# Fake Store API Client 🛒

A Spring Boot application that consumes the [Fake Store API](https://fakestoreapi.com) using `RestTemplate`.

## 🛠 Tech Stack
* **Java 21**
* **Spring Boot 3**
* **RestTemplate**

## 💡 Key Concepts
* **REST Client:** Consuming external APIs with `RestTemplate`.
* **Nested DTOs:** Mapping complex JSON (`Product` -> `Rating`).
* **Stream API:** Filtering products by price, rating, and search keywords.
